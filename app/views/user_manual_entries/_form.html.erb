<%# locals: (user_manual_heading:, user_manual_entry:) %>

<%= form_with(model: [user_manual_heading, user_manual_entry]) do |form| %>
  <% if user_manual_heading.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_manual_entry.errors.count, "error") %> prohibited this user_manual_entry from being saved:</h2>

      <ul>
        <% user_manual_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
